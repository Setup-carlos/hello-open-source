name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Lint
        run: echo "lint placeholder - replace with real command"

      - name: Test
        run: echo "test placeholder - replace with real command"

      - name: Build
        run: echo "build placeholder - replace with real command"
